# Task 72. Test-only dependencies — not needed to run the app itself
# (see requirements.txt for that). Install both to run the suite:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest

pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1
